diff -up shadow-4.1.4.2/man/useradd.8.uadd-man shadow-4.1.4.2/man/useradd.8
--- shadow-4.1.4.2/man/useradd.8.uadd-man	2014-06-05 13:59:16.181381432 +0200
+++ shadow-4.1.4.2/man/useradd.8	2014-06-05 13:53:48.512709426 +0200
@@ -9,6 +9,15 @@
 .\"
 .TH "USERADD" "8" "07/24/2009" "System Management Commands" "System Management Commands"
 .\" -----------------------------------------------------------------
+.\" * Define some portability stuff
+.\" -----------------------------------------------------------------
+.\" ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
+.\" http://bugs.debian.org/507673
+.\" http://lists.gnu.org/archive/html/groff/2009-02/msg00013.html
+.\" ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
+.ie \n(.g .ds Aq \(aq
+.el       .ds Aq '
+.\" -----------------------------------------------------------------
 .\" * set default formatting
 .\" -----------------------------------------------------------------
 .\" disable hyphenation
@@ -35,7 +44,7 @@ option, the
 \fBuseradd\fR
 command creates a new user account using the values specified on the command line plus the default values from the system\&. Depending on command line options, the
 \fBuseradd\fR
-command will update system files and may also create the new user\'s home directory and copy initial files\&.
+command will update system files and may also create the new user\*(Aqs home directory and copy initial files\&.
 .PP
 By default, a group will also be created for the new user (see
 \fB\-g\fR,
@@ -71,14 +80,14 @@ by default\&.
 .PP
 \fB\-c\fR, \fB\-\-comment\fR \fICOMMENT\fR
 .RS 4
-Any text string\&. It is generally a short description of the login, and is currently used as the field for the user\'s full name\&.
+Any text string\&. It is generally a short description of the login, and is currently used as the field for the user\*(Aqs full name\&.
 .RE
 .PP
 \fB\-d\fR, \fB\-\-home\fR \fIHOME_DIR\fR
 .RS 4
 The new user will be created using
 \fIHOME_DIR\fR
-as the value for the user\'s login directory\&. The default is to append the
+as the value for the user\*(Aqs login directory\&. The default is to append the
 \fILOGIN\fR
 name to
 \fIBASE_DIR\fR
@@ -119,9 +128,9 @@ variable in
 .PP
 \fB\-g\fR, \fB\-\-gid\fR \fIGROUP\fR
 .RS 4
-The group name or number of the user\'s initial login group\&. The group name must exist\&. A group number must refer to an already existing group\&.
+The group name or number of the user\*(Aqs initial login group\&. The group name must exist\&. A group number must refer to an already existing group\&.
 .sp
-If not specified, the bahavior of
+If not specified, the behavior of
 \fBuseradd\fR
 will depend on the
 \fBUSERGROUPS_ENAB\fR
@@ -154,7 +163,7 @@ Display help message and exit\&.
 .PP
 \fB\-k\fR, \fB\-\-skel\fR \fISKEL_DIR\fR
 .RS 4
-The skeleton directory, which contains files and directories to be copied in the user\'s home directory, when the home directory is created by
+The skeleton directory, which contains files and directories to be copied in the user\*(Aqs home directory, when the home directory is created by
 \fBuseradd\fR\&.
 .sp
 This option is only valid if the
@@ -200,21 +209,21 @@ doesn\'t work yet\&.
 .RS 4
 Do not add the user to the lastlog and faillog databases\&.
 .sp
-By default, the user\'s entries in the lastlog and faillog databases are resetted to avoid reusing the entry from a previously deleted user\&.
+By default, the user\*(Aqs entries in the lastlog and faillog databases are resetted to avoid reusing the entry from a previously deleted user\&.
 .RE
 .PP
 \fB\-m\fR, \fB\-\-create\-home\fR
 .RS 4
-Create the user\'s home directory if it does not exist\&. The files and directories contained in the skeleton directory (which can be defined with the
+Create the user\*(Aqs home directory if it does not exist\&. The files and directories contained in the skeleton directory (which can be defined with the
 \fB\-k\fR
 option) will be copied to the home directory\&.
 .sp
-By default, no home directories are created\&.
+useradd will create the home directory unless \fBCREATE_HOME\fR in /etc/login.defs is set to no.
 .RE
 .PP
 \fB\-M\fR
 .RS 4
-Do no create the user\'s home directory, even if the system wide setting from
+Do not create the user\*(Aqs home directory, even if the system wide setting from
 /etc/login\&.defs
 (\fBCREATE_HOME\fR) is set to
 \fIyes\fR\&.
@@ -244,7 +253,7 @@ variable in
 Allow the creation of a user account with a duplicate (non\-unique) UID\&.
 .sp
 This option is only valid in combination with the
-\fB\-o\fR
+\fB\-u\fR
 option\&.
 .RE
 .PP
@@ -257,7 +266,7 @@ The encrypted password, as returned by
 \fBNote:\fR
 This option is not recommended because the password (or encrypted password) will be visible by users listing the processes\&.
 .sp
-You should make sure the password respects the system\'s password policy\&.
+You should make sure the password respects the system\*(Aqs password policy\&.
 .RE
 .PP
 \fB\-r\fR, \fB\-\-system\fR
@@ -265,7 +274,7 @@ You should make sure the password respec
 Create a system account\&.
 .sp
 System users will be created with no aging information in
-/etc/shadow, and their numeric identifiers are choosen in the
+/etc/shadow, and their numeric identifiers are chosen in the
 \fBSYS_UID_MIN\fR\-\fBSYS_UID_MAX\fR
 range, defined in
 /etc/login\&.defs, instead of
@@ -285,7 +294,7 @@ options if you want a home directory for
 .PP
 \fB\-s\fR, \fB\-\-shell\fR \fISHELL\fR
 .RS 4
-The name of the user\'s login shell\&. The default is to leave this field blank, which causes the system to select the default login shell specified by the
+The name of the user\*(Aqs login shell\&. The default is to leave this field blank, which causes the system to select the default login shell specified by the
 \fBSHELL\fR
 variable in
 /etc/default/useradd, or an empty string by default\&.
@@ -293,9 +302,17 @@ variable in
 .PP
 \fB\-u\fR, \fB\-\-uid\fR \fIUID\fR
 .RS 4
-The numerical value of the user\'s ID\&. This value must be unique, unless the
+The numerical value of the user\*(Aqs ID\&. This value must be unique, unless the
 \fB\-o\fR
-option is used\&. The value must be non\-negative\&. The default is to use the smallest ID value greater than 999 and greater than every other user\&. Values between 0 and 999 are typically reserved for system accounts\&.
+option is used\&. The value must be non\-negative\&. The default is to use the smallest ID value greater than or equal to
+\fBUID_MIN\fR
+and greater than every other user\&.
+.sp
+See also the
+\fB\-r\fR
+option and the
+\fBUID_MAX\fR
+description\&.
 .RE
 .PP
 \fB\-U\fR, \fB\-\-user\-group\fR
@@ -314,7 +331,7 @@ variable in
 .PP
 \fB\-Z\fR, \fB\-\-selinux\-user\fR \fISEUSER\fR
 .RS 4
-The SELinux user for the user\'s login\&. The default is to leave this field blank, which causes the system to select the default SELinux user\&.
+The SELinux user for the user\*(Aqs login\&. The default is to leave this field blank, which causes the system to select the default SELinux user\&.
 .RE
 .SS "Changing the default values"
 .PP
@@ -330,9 +347,9 @@ will update the default values for the s
 .PP
 \fB\-b\fR, \fB\-\-base\-dir\fR \fIBASE_DIR\fR
 .RS 4
-The path prefix for a new user\'s home directory\&. The user\'s name will be affixed to the end of
+The path prefix for a new user\*(Aqs home directory\&. The user\*(Aqs name will be affixed to the end of
 \fIBASE_DIR\fR
-to form the new user\'s home directory name, if the
+to form the new user\*(Aqs home directory name, if the
 \fB\-d\fR
 option is not used when creating a new account\&.
 .sp
@@ -364,14 +381,14 @@ variable in
 .PP
 \fB\-g\fR, \fB\-\-gid\fR \fIGROUP\fR
 .RS 4
-The group name or ID for a new user\'s initial group (when the
+The group name or ID for a new user\*(Aqs initial group (when the
 \fB\-N/\-\-no\-user\-group\fR
 is used or when the
 \fBUSERGROUPS_ENAB\fR
 variable is set to
 \fIno\fR
 in
-/etc/login\&.defs\&. The named group must exist, and a numerical group ID must have an existing entry\&.
+/etc/login\&.defs)\&. The named group must exist, and a numerical group ID must have an existing entry\&.
 .sp
 This option sets the
 \fBGROUP\fR
@@ -381,7 +398,7 @@ variable in
 .PP
 \fB\-s\fR, \fB\-\-shell\fR \fISHELL\fR
 .RS 4
-The name of a new user\'s login shell\&.
+The name of a new user\*(Aqs login shell\&.
 .sp
 This option sets the
 \fBSHELL\fR
@@ -414,7 +431,7 @@ change the behavior of this tool:
 .RS 4
 Indicate if a home directory should be created by default for new users\&.
 .sp
-This setting does not apply to system users, and can be overriden on the command line\&.
+This setting does not apply to system users, and can be overridden on the command line\&.
 .RE
 .PP
 \fBGID_MAX\fR (number), \fBGID_MIN\fR (number)
@@ -443,7 +460,7 @@ variables are used by
 \fBuseradd\fR,
 \fBusermod\fR, and
 \fBuserdel\fR
-to create, move, or delete the user\'s mail spool\&.
+to create, move, or delete the user\*(Aqs mail spool\&.
 .PP
 If
 \fBMAIL_CHECK_ENAB\fR
@@ -518,7 +535,7 @@ use this mask to set the mode of the hom
 .sp
 It is also used by
 \fBlogin\fR
-to define users\' initial umask\&. Note that this mask can be overriden by the user\'s GECOS line (if
+to define users\*(Aq initial umask\&. Note that this mask can be overridden by the user\*(Aqs GECOS line (if
 \fBQUOTAS_ENAB\fR
 is set) or by the specification of a limit with the
 \fIK\fR
@@ -533,7 +550,7 @@ Enable setting of the umask group bits t
 If set to
 \fIyes\fR,
 \fBuserdel\fR
-will remove the user\'s group if it contains no more members, and
+will remove the user\*(Aqs group if it contains no more members, and
 \fBuseradd\fR
 will create by default a group with the name of the user\&.
 .RE
@@ -586,7 +603,7 @@ success
 .PP
 \fI1\fR
 .RS 4
-can\'t update password file
+can\*(Aqt update password file
 .RE
 .PP
 \fI2\fR
@@ -607,7 +624,7 @@ UID already in use (and no
 .PP
 \fI6\fR
 .RS 4
-specified group doesn\'t exist
+specified group doesn\*(Aqt exist
 .RE
 .PP
 \fI9\fR
@@ -617,22 +634,22 @@ username already in use
 .PP
 \fI10\fR
 .RS 4
-can\'t update group file
+can\*(Aqt update group file
 .RE
 .PP
 \fI12\fR
 .RS 4
-can\'t create home directory
+can\*(Aqt create home directory
 .RE
 .PP
 \fI13\fR
 .RS 4
-can\'t create mail spool
+can\*(Aqt create mail spool
 .RE
 .PP
 \fI14\fR
 .RS 4
-can\'t update SELinux user mapping
+can\*(Aqt update SELinux user mapping
 .RE
 .SH "SEE ALSO"
 .PP
